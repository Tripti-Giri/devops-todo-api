version: '3.8'

services:
  todo-api:
    build: ./              
    container_name: todo-api
    ports:
      - "5000:5000"            
    environment:
      - FLASK_ENV=development
    restart: unless-stopped    